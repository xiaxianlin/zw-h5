import { AssetElement } from "@shared/components";

interface BlockProps {}
export function Block05({}: BlockProps) {
  return <AssetElement className="relative" assetKey="05badayun-bg" width={750} height={1334}></AssetElement>;
}

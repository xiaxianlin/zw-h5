import { AssetElement } from "@shared/components";

interface BlockProps {}
export function Block06({}: BlockProps) {
  return <AssetElement className="relative" assetKey="06luohualiushui-bg" width={750} height={1334}></AssetElement>;
}

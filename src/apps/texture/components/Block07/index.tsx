import { AssetElement } from "@shared/components";

interface BlockProps {}
export function Block07({}: BlockProps) {
  return <AssetElement className="relative" assetKey="07anniu-bg" width={750} height={625}></AssetElement>;
}

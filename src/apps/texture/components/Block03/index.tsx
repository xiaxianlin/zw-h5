import { AssetElement } from "@shared/components";

interface BlockProps {}
export function Block03({}: BlockProps) {
  return <AssetElement className="relative" assetKey="03canzhimudan-bg" width={750} height={1334}></AssetElement>;
}

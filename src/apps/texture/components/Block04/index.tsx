import { AssetElement } from "@shared/components";

interface BlockProps {}
export function Block04({}: BlockProps) {
  return <AssetElement className="relative" assetKey="04yinianjing-bg" width={750} height={1334}></AssetElement>;
}
